/*
    This code adds the values of
    two variables and alerts the results.
*/

var firstNum = 0; // first variable
var secondNum = 12; // second variable
var result = firstNum + secondNum; // calculate the sum
// Alert the result
console.log('result=' + result);
debugger;
alert(result);
